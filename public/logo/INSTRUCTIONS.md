Substitua este placeholder `chicogrill.png` pelo arquivo de logo real fornecido na conversa.

Nome do arquivo final: `chicogrill.png`
Caminho: `public/logo/chicogrill.png`

Passos:
1. Copie o arquivo de imagem do logo (por exemplo `chicogrill.png`) para `public/logo/`.
2. Mantenha o mesmo nome do arquivo.
3. Reinicie o servidor de desenvolvimento se estiver rodando (`npm run dev` / `pnpm dev`).
4. Atualize o tamanho no componente `components/Navbar.tsx` se necessário (props `width`/`height`).

O header agora referencia `/logo/chicogrill.png` em `components/Navbar.tsx`.